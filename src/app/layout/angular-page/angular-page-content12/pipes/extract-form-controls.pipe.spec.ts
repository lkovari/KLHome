import { ExtractFormControlsPipe } from './extract-form-controls.pipe';

describe('ExtractFormControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractFormControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
